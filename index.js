const sum = (a, b) => {
  return a + b;
};

const subtraction = (c, d) => {
  return c - d;
};

const product = (e, f) => {
  return e * f;
};

const division = (g, h) => {
  return g / h;
};

const myBeverage = {
  delicious: true,
  sour: false,
};

module.exports = {
  sum,
  subtraction,
  product,
  division,
  myBeverage,
};
